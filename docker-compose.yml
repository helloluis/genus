services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: genus
      POSTGRES_USER: genus
      POSTGRES_PASSWORD: genus_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
